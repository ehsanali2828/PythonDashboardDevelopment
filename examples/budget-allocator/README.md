# Budget Allocator

Demonstrates **custom action handlers** (`CallHandler`). Five budget sliders are constrained to sum to 100% — moving one proportionally redistributes the others client-side with zero server round-trips.

```bash
uv run prefab serve examples/budget-allocator/app.py
```
