# Custom Pipes

Demonstrates **custom expression pipes** (`js_pipes`). Three JavaScript formatting functions — temperature with emoji, star ratings, and initials — used live in `{{ }}` expressions with reactive sliders.

```bash
uv run prefab serve examples/custom-pipes/app.py
```
