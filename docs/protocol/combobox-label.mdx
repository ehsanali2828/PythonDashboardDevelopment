---
title: ComboboxLabel
description: JSON Schema for the ComboboxLabel component.
---

A label/header for a `ComboboxGroup`.

```json
{
  "type": "object",
  "properties": {
    "type": {
      "const": "ComboboxLabel",
      "default": "ComboboxLabel",
      "type": "string"
    },
    "id": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "HTML id attribute for CSS targeting. Applied to the outermost element."
    },
    "cssClass": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "CSS/Tailwind classes for styling. Accepts a Responsive() for breakpoint-aware classes."
    },
    "onMount": {
      "default": null,
      "description": "Action(s) to execute when this component mounts."
    },
    "label": {
      "description": "Label text",
      "type": "string"
    }
  },
  "required": [
    "type",
    "label"
  ]
}
```
